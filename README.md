# Telegram Bot для системы обратной связи InvestInKids

Телеграм-бот для приема и обработки жалоб, предложений и обратной связи с поддержкой загрузки фотографий.

## 🚀 Возможности

- ✅ Создание обращений с фото или без фото
- ✅ Категоризация обращений (проблемы, предложения, обратная связь)
- ✅ Подкатегории для проблем (Помещение/оборудование, Учебный процесс, Персонал)
- ✅ Система управления статусами обращений
- ✅ Назначение ответственных администраторов
- ✅ Автоматические напоминания о необработанных обращениях
- ✅ История обращений пользователя
- ✅ Reply-клавиатуры для удобной навигации
- ✅ Fallback-обработчик для неизвестных команд (не перехватывает команды админов)
- ✅ Детальное логирование в файлы с ротацией

## 📋 Требования

- Python 3.8+
- PostgreSQL 12+
- Telegram Bot Token
- Telegram Group ID для администраторов

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd investinkids-telegram-bot-main/investinkids-telegram-bot
```

### 2. Установка зависимостей

```bash
pip install aiogram psycopg2-binary python-dotenv
```

Или используйте `requirements.txt` (если создан):

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token
ADMIN_GROUP_ID=your_admin_group_id
MAIN_ADMIN_ID=your_main_admin_id

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=investinkids
DB_USER=postgres
DB_PASSWORD=your_database_password
```

**Как получить BOT_TOKEN:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**Как получить ADMIN_GROUP_ID:**
1. Добавьте бота в группу администраторов
2. Отправьте любое сообщение в группу
3. Перейдите по ссылке: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
4. Найдите `"chat":{"id":-123456789}` - это и есть ID группы (с минусом!)

### 4. Создание базы данных

```sql
CREATE DATABASE investinkids;
```

Или через командную строку PostgreSQL:

```bash
createdb investinkids
```

### 5. Запуск бота

```bash
python main.py
```

Бот автоматически создаст необходимые таблицы при первом запуске.

## 📖 Использование

### Для пользователей

1. **Запуск бота**: Отправьте команду `/start`
2. **Выбор типа обращения**:
   - 🔧 Сообщить о проблеме
   - 💡 Оставить предложение
   - 💬 Обратная связь
3. **Для проблем**: Выберите подкатегорию:
   - 🏫 Помещение/оборудование
   - 📚 Учебный процесс
   - 👥 Персонал
4. **Добавление фото** (опционально):
   - 📷 Добавить фото
   - ➡️ Продолжить без фото
5. **Описание проблемы**: Введите подробное описание
6. **Отслеживание**: Используйте "📋 Мои обращения" для просмотра статуса

### Для администраторов

#### Команды:

| Команда | Описание |
|---------|----------|
| `/pending` | Показать все ожидающие обращения |
| `/inprogress` | Показать обращения в работе |
| `/completed` | Показать завершенные обращения |
| `/report_[ID]` | Детали конкретного обращения |
| `/take_[ID]` | Взять обращение в работу |
| `/complete_[ID] [ответ]` | Завершить обращение с ответом |
| `/adminhelp` | Справка по командам |

#### Примеры использования:

```
/take_123
/complete_123 Проблема решена, заменили оборудование
/report_123
```

#### Процесс работы администратора:

1. Новое обращение приходит в админ-группу
2. Администратор использует `/take_123` для взятия в работу
3. Бот отправляет детали обращения администратору в личные сообщения
4. После решения проблемы: `/complete_123 [ответ]`
5. Пользователь автоматически получает уведомление с ответом

**Важно:** Все команды админов работают как в группе, так и в личных сообщениях с ботом.

## 🗂️ Структура проекта

```
investinkids-telegram-bot/
├── main.py              # Точка входа, инициализация бота и логирования
├── config.py            # Конфигурация (токены, БД, переменные окружения)
├── scheduler.py         # Планировщик напоминаний о необработанных обращениях
├── handlers/
│   ├── __init__.py
│   ├── user.py         # Обработчики для пользователей
│   └── admin.py        # Обработчики для администраторов
├── keyboards/
│   ├── __init__.py
│   └── reply_kb.py     # Reply-клавиатуры для навигации
├── db/
│   ├── __init__.py
│   └── queries.py      # Запросы к базе данных
├── logs/               # Папка с логами (создается автоматически)
│   ├── bot.log        # Основной лог-файл
│   └── errors.log     # Лог ошибок
└── .env               # Переменные окружения (создать вручную)
```

## 🗄️ База данных

### Таблица `reports`

Бот использует PostgreSQL с таблицей `reports`, которая содержит:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | SERIAL | Уникальный ID обращения |
| `user_id` | BIGINT | Telegram ID пользователя |
| `user_name` | TEXT | Имя пользователя |
| `report_type` | TEXT | Тип обращения |
| `report_text` | TEXT | Текст обращения |
| `photo_file_id` | TEXT | ID фото в Telegram (опционально) |
| `status` | TEXT | Статус: `pending`, `in_progress`, `completed` |
| `responsible_user_id` | BIGINT | ID ответственного администратора |
| `responsible_user_name` | TEXT | Имя ответственного администратора |
| `admin_response` | TEXT | Ответ администратора |
| `created_at` | TIMESTAMP | Время создания |
| `taken_at` | TIMESTAMP | Время взятия в работу |
| `completed_at` | TIMESTAMP | Время завершения |

### Индексы

Для оптимизации запросов созданы индексы:
- `idx_reports_user_id` - по user_id
- `idx_reports_status` - по status
- `idx_reports_responsible` - по responsible_user_id

## 📝 Статусы обращений

- **`pending`** - Ожидает обработки (⏳)
- **`in_progress`** - В работе (🔄)
- **`completed`** - Завершено (✅)

## 🔔 Автоматические напоминания

Бот автоматически отправляет напоминания в админ-группу о обращениях, которые ожидают обработки более 1 часа. 

- **Проверка**: каждые 30 минут
- **Условие**: обращения со статусом `pending` старше 1 часа
- **Формат**: напоминание с деталями обращения и командой для взятия в работу

## 📊 Логирование

Бот ведет детальное логирование всех событий в файлы:

### Файлы логов

- **`logs/bot.log`** - Основной лог-файл (все события уровня INFO и выше)
- **`logs/errors.log`** - Лог ошибок (только ERROR)

### Ротация логов

- **Максимальный размер файла**: 10 МБ
- **Количество бэкапов**: 5 файлов
- **Кодировка**: UTF-8

### Просмотр логов на сервере

```bash
# Последние 50 строк основного лога
tail -n 50 logs/bot.log

# Следить за логами в реальном времени
tail -f logs/bot.log

# Поиск ошибок
grep "ERROR" logs/bot.log

# Просмотр лога ошибок
cat logs/errors.log

# Поиск по конкретному пользователю
grep "user_id: 123456789" logs/bot.log
```

### Уровни логирования

- **INFO** - Общая информация (запуск бота, создание обращений)
- **WARNING** - Предупреждения (aiogram)
- **ERROR** - Ошибки (записываются в отдельный файл)

## 🛠️ Технологии

- **aiogram 3.x** - Асинхронный фреймворк для Telegram Bot API
- **PostgreSQL** - Реляционная база данных
- **psycopg2** - Адаптер для работы с PostgreSQL
- **python-dotenv** - Загрузка переменных окружения
- **logging** - Встроенная библиотека Python для логирования

## 🔒 Безопасность

- ✅ Переменные окружения для чувствительных данных
- ✅ Валидация конфигурации при запуске
- ✅ Защита от SQL-инъекций (параметризованные запросы)
- ✅ Логирование без чувствительных данных

## 🚨 Устранение неполадок

### Бот не запускается

1. Проверьте наличие файла `.env`
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте подключение к базе данных:
   ```bash
   psql -h localhost -U postgres -d investinkids
   ```

### Ошибки подключения к БД

- Проверьте правильность данных в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа пользователя БД

### Бот не отвечает

- Проверьте логи: `tail -f logs/bot.log`
- Убедитесь, что бот добавлен в админ-группу
- Проверьте правильность `ADMIN_GROUP_ID`

### Команды админов не работают

- Убедитесь, что бот добавлен в группу администраторов
- Проверьте, что используете правильный формат команды (например, `/take_123`)
- Проверьте логи на наличие ошибок

### Проблемы с фото

- Telegram хранит фото по file_id
- Фото доступны только если бот имеет к ним доступ
- При перезапуске бота старые фото могут стать недоступными

## 📝 Развертывание на сервере

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/investinkids-bot.service`:

```ini
[Unit]
Description=InvestInKids Telegram Bot
After=network.target postgresql.service

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/investinkids-telegram-bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:

```bash
sudo systemctl enable investinkids-bot
sudo systemctl start investinkids-bot
sudo systemctl status investinkids-bot
```

### Использование screen/tmux

```bash
# Создать новую сессию
screen -S bot
# или
tmux new -s bot

# Запустить бота
python main.py

# Отключиться (Ctrl+A, затем D для screen, Ctrl+B, затем D для tmux)
# Подключиться обратно: screen -r bot или tmux attach -t bot
```

## 🔄 Обновления

При обновлении бота:
1. Остановите бота
2. Сделайте бэкап базы данных
3. Обновите код
4. Проверьте миграции БД (если есть)
5. Запустите бота
6. Проверьте логи на наличие ошибок

## 📄 Лицензия

Этот проект создан для InvestInKids.

## 👥 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию в `.env`
4. Создайте issue в репозитории проекта с описанием проблемы и логами

---

**Версия**: 1.1  
**Последнее обновление**: 2024

